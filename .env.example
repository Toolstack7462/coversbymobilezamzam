# Example only. Copy to .env for tooling that reads it.
# NEVER put real values in this file - it is committed.
# Runtime secrets for Workers belong in .dev.vars (local) or
# `wrangler secret put` (deployed). See docs/deployment.md.

# Used by drizzle-kit when generating/inspecting against a remote D1.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=
